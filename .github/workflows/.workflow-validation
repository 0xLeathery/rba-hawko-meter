GitHub Actions Workflow Validation - 2026-07-18 (Phase 4 recovery)

VALIDATED ACTION MAJORS:
- actions/checkout@v6
- actions/setup-python@v6
- actions/setup-node@v4 (CI only)
- actions/github-script@v7 (failure reporting)
- stefanzweifel/git-auto-commit-action@v7

WORKFLOWS:
- ci.yml — PR/push: npm run verify:fast (lint + pytest not-live + coverage ≥85%)
- weekly-pipeline.yml — schedule + dispatch; issues:write for pipeline-failure
- daily-asx-futures.yml — weekday ASX + meetings refresh
- live-canary.yml — Mon/Thu live pytest; opens pipeline-failure issue on fail

NOTES:
- npm ci --ignore-scripts in CI (skip local lefthook postinstall)
- data commits use [skip ci] to avoid loops
- critical pipeline failure still refreshes meetings/status (see pipeline/main.py)
